#install.packages("sqldf")
library(sqldf)
sql_binary_goodbad <- sqldf("select 
                      from 
                      where ")

#Exercise1: we want the "good" only
sql_only_good <- sqldf("")
#Exercise2: we want the "bad" only
sql_only_bad <- sqldf("")
#Exercise3: we want all obs. with purpose above 3
sql_count_purposeabove3 <- sqldf("")